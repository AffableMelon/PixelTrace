# Group 2 Pixeltrace DataVisTool

cd into backend
run npm install to install all dependencies
run npm run dev to start the backend in dev mode.

do the same for the frontend too but just cd into frontend not backend!
